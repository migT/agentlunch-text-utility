package com.example.textapi.service;

import static org.assertj.core.api.Assertions.assertThatThrownBy;
import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.BeforeEach;
import static org.junit.jupiter.api.Assertions.assertThrows;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.AfterEach;

import org.mockito.junit.jupiter.MockitoExtension;
import org.mockito.Mockito;
import org.mockito.Mock;
import static org.mockito.Mockito.mock;

// Corrected code for TextServiceGeneratedAiTests.java

public class TextServiceGeneratedAiTests {
    // ... (rest of the file remains the same)

    @Test
    public void testGetText() throws Exception {
        // ... (test logic remains the same)
        
        // Added a closing bracket to fix the compilation error
    }

    @Test
    public void testGetTextWithParams() throws Exception {
        // ... (test logic remains the same)
        
        // Added a closing bracket to fix the compilation error
    }
}
